package com.yc.parkcharge2.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yc.parkcharge2.entity.StrateLong;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "STRATE_LONG".
*/
public class StrateLongDao extends AbstractDao<StrateLong, Long> {

    public static final String TABLENAME = "STRATE_LONG";

    /**
     * Properties of entity StrateLong.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property FirstHHPrice = new Property(1, double.class, "firstHHPrice", false, "FIRST_HHPRICE");
        public final static Property SecondHHPrice = new Property(2, double.class, "secondHHPrice", false, "SECOND_HHPRICE");
    };


    public StrateLongDao(DaoConfig config) {
        super(config);
    }
    
    public StrateLongDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"STRATE_LONG\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"FIRST_HHPRICE\" REAL NOT NULL ," + // 1: firstHHPrice
                "\"SECOND_HHPRICE\" REAL NOT NULL );"); // 2: secondHHPrice
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"STRATE_LONG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, StrateLong entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getFirstHHPrice());
        stmt.bindDouble(3, entity.getSecondHHPrice());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, StrateLong entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindDouble(2, entity.getFirstHHPrice());
        stmt.bindDouble(3, entity.getSecondHHPrice());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public StrateLong readEntity(Cursor cursor, int offset) {
        StrateLong entity = new StrateLong( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getDouble(offset + 1), // firstHHPrice
            cursor.getDouble(offset + 2) // secondHHPrice
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, StrateLong entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFirstHHPrice(cursor.getDouble(offset + 1));
        entity.setSecondHHPrice(cursor.getDouble(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(StrateLong entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(StrateLong entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
