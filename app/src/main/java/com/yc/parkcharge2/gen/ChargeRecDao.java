package com.yc.parkcharge2.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yc.parkcharge2.entity.ChargeRec;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHARGE_REC".
*/
public class ChargeRecDao extends AbstractDao<ChargeRec, Long> {

    public static final String TABLENAME = "CHARGE_REC";

    /**
     * Properties of entity ChargeRec.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CarNo = new Property(1, String.class, "carNo", false, "CAR_NO");
        public final static Property CarType = new Property(2, Integer.class, "carType", false, "CAR_TYPE");
        public final static Property StartTime = new Property(3, java.util.Date.class, "startTime", false, "START_TIME");
        public final static Property EndTime = new Property(4, java.util.Date.class, "endTime", false, "END_TIME");
        public final static Property Charges = new Property(5, double.class, "charges", false, "CHARGES");
        public final static Property Sign = new Property(6, String.class, "sign", false, "SIGN");
        public final static Property ParkRecId = new Property(7, String.class, "parkRecId", false, "PARK_REC_ID");
    };


    public ChargeRecDao(DaoConfig config) {
        super(config);
    }
    
    public ChargeRecDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHARGE_REC\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CAR_NO\" TEXT," + // 1: carNo
                "\"CAR_TYPE\" INTEGER," + // 2: carType
                "\"START_TIME\" INTEGER," + // 3: startTime
                "\"END_TIME\" INTEGER," + // 4: endTime
                "\"CHARGES\" REAL NOT NULL ," + // 5: charges
                "\"SIGN\" TEXT," + // 6: sign
                "\"PARK_REC_ID\" TEXT);"); // 7: parkRecId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHARGE_REC\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChargeRec entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String carNo = entity.getCarNo();
        if (carNo != null) {
            stmt.bindString(2, carNo);
        }
 
        Integer carType = entity.getCarType();
        if (carType != null) {
            stmt.bindLong(3, carType);
        }
 
        java.util.Date startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindLong(4, startTime.getTime());
        }
 
        java.util.Date endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindLong(5, endTime.getTime());
        }
        stmt.bindDouble(6, entity.getCharges());
 
        String sign = entity.getSign();
        if (sign != null) {
            stmt.bindString(7, sign);
        }
 
        String parkRecId = entity.getParkRecId();
        if (parkRecId != null) {
            stmt.bindString(8, parkRecId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChargeRec entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String carNo = entity.getCarNo();
        if (carNo != null) {
            stmt.bindString(2, carNo);
        }
 
        Integer carType = entity.getCarType();
        if (carType != null) {
            stmt.bindLong(3, carType);
        }
 
        java.util.Date startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindLong(4, startTime.getTime());
        }
 
        java.util.Date endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindLong(5, endTime.getTime());
        }
        stmt.bindDouble(6, entity.getCharges());
 
        String sign = entity.getSign();
        if (sign != null) {
            stmt.bindString(7, sign);
        }
 
        String parkRecId = entity.getParkRecId();
        if (parkRecId != null) {
            stmt.bindString(8, parkRecId);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ChargeRec readEntity(Cursor cursor, int offset) {
        ChargeRec entity = new ChargeRec( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // carNo
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // carType
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // startTime
            cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)), // endTime
            cursor.getDouble(offset + 5), // charges
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // sign
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // parkRecId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChargeRec entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCarNo(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCarType(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setStartTime(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setEndTime(cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)));
        entity.setCharges(cursor.getDouble(offset + 5));
        entity.setSign(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setParkRecId(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ChargeRec entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ChargeRec entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
